import 'package:flutter/material.dart';

class Sign_up_rep extends StatelessWidget {
  const Sign_up_rep({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Container(child: Text("page sign up after click in button skip"))),
    );
  }
}