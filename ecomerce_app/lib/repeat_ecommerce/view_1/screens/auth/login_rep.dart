import 'package:flutter/material.dart';

class Login_rep extends StatelessWidget {
  const Login_rep({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Container(child: Text("login page of ecommerce repp"))),
    );
  }
}