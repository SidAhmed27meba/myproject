import 'package:flutter/material.dart';

class Sccess_password extends StatelessWidget {
  const Sccess_password({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}